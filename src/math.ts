
/**
 * Returns the input multiplied by 4.
 */
export function quadruple(n: number): number {
  return n * 4;
}
export function quadruple(n: number): number {
  return n * 4;
}
/**
 * Returns the input multiplied by 4.
 */
export function quadruple(n: number): number {
  return n * 4;
}
