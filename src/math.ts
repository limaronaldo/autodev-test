
/**
 * Returns the arithmetic product of two numbers.
 */
export function multiply(a: number, b: number): number {
  return a * b;
}
export function multiply(a: number, b: number): number {
  return a * b;
}